apiVersion: apps/v1
kind: Deployment
metadata:
  name: gps-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gps
  template:
    metadata:
      labels:
        app: gps
    spec:
      containers:
      - name: gps-container
        image: gps-service
        imagePullPolicy: Never
        ports:
        - containerPort: 5000

---
apiVersion: v1
kind: Service
metadata:
  name: gps-service
spec:
  type: NodePort
  selector:
    app: gps
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30008